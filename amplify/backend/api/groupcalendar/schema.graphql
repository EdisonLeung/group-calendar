type Event @model @auth(rules: [{allow: public, operations: [read]}, {allow: owner}, {allow: private, operations: [read]}]) {
  id: ID!
  title: String!
  startTime: String
  endTime: String
  repeat: Boolean
  allDay: Boolean
  daysOfWeek: [Int]
}